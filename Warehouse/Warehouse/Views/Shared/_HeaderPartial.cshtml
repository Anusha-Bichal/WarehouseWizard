<header class="bg-white border-b shadow mb-1">
    <div class="container mx-auto px-4 py-2 flex justify-between items-center">
        <a href="@Url.Action("Index", "Home")" class="text-xl font-bold text-black hover:text-gray-700">Warehouse</a>

        <button type="button"
                class="lg:hidden block"
                data-bs-toggle="collapse"
                data-bs-target=".navbar-collapse"
                aria-controls="navbarSupportedContent"
                aria-expanded="false"
                aria-label="Toggle navigation">
            <svg class="w-6 h-6 fill-current" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20">
                <path d="M2 6h16v2H2V6zm0 5h16v2H2v-2zm0 5h16v2H2v-2z"/>
            </svg>
        </button>

        <div class="hidden lg:flex items-center space-x-4">
            <a href="@Url.Action("Index", "Auth")" class="text-black hover:text-gray-700">Authentication</a>
            <a href="@Url.Action("Index", "Employee")" class="text-black hover:text-gray-700">Employee</a>
            <a href="@Url.Action("Index", "Owner")" class="text-black hover:text-gray-700">Owner</a>
        </div>
    </div>
</header>